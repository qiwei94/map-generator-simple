# Map Generator - Python Dependencies
# Python version: 3.9.6

# Core dependencies
numpy>=1.24.0
trimesh>=4.0.0
shapely>=2.0.0
geopandas>=0.14.0
pandas>=2.0.0
osmnx>=2.0.0
pyproj>=3.6.0
requests>=2.28.0
scipy>=1.11.0
rich>=13.0.0

# Optional (recommended for better performance)
mapbox-earcut>=1.0.0

# Local DEM/GeoTIFF reader — used by elevation.py for the Copernicus DEM path.
# Optional: if not installed, elevation.py silently falls back to SRTM HGT.
# >=1.4 bundles GDAL in the wheel (no `apt install gdal-bin` needed).
rasterio>=1.4.0

# OSM 提取（tools/osmium_pyosmium.py shim 的运行时依赖）。
# 注意：`pyosmium` 已从 PyPI 下架（404），继任包名就是 `osmium`（同一库，
# 4.x 起改名，支持 Python 3.9–3.14，Windows 有 cp314 wheel）。
# 清华镜像不同步此包，需官方源安装：
#   pip install -i https://pypi.org/simple "osmium>=4.0"
osmium>=4.0

# 3D manifold mesh library — guaranteed watertight output for 3D printing
manifold3d>=3.4.0

# Data validation & settings management — strict type checking for city profiles
pydantic>=2.0.0

# VLM aesthetic review (optional — aesthetic loop degrades to pure metrics without it)
openai>=1.0.0
python-dotenv>=1.0.0

# ── Web 前端服务（webapp/：旅程回忆 Studio）──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9      # 照片上传
Pillow>=10.0.0               # EXIF GPS / 时间提取

# ── 测试 ──
pytest>=7.0.0
psutil>=5.9.0                # 资源占用基准测试