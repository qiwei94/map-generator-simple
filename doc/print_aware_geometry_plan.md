# 打印感知的城市浮雕重构方案

状态：P0 已完成；P1 道路职责分离已实现，正在进行多城市默认值验收
工作分支：`agent/print-aware-foundation`

## 1. 目标与边界

当前管线已经零散考虑了喷嘴、道路宽度、水体宽度、建筑高度和材料颜色，
但这些约束分散在 `config.py`、自动参数、画廊 preset 和预处理函数中；部分参数
虽然被解析，却没有抵达实际几何路径。结果是同一套参数在 5、15、25 km 取景
中代表完全不同的可打印尺度，也容易出现“路网负责切块，同时又全部被画出来”
造成的黑、密、乱。

本次重构要建立一条可审计的确定性链路：

```text
PrinterProfile + 真实 bbox + 成品尺寸
        ↓
PrintScale / PrintConstraints
        ↓
城市特征分类与视觉焦点策略
        ↓
拓扑网络 → 结构缝网络 → 可见道路/水体
        ↓
语义 Z 量化 → 网格 → 3MF
        ↓
PrintReport + DesignSpec + 验证器
```

边界：

- LLM/VLM 只能建议场景类型、视觉焦点和审美权重，不能直接控制顶点、全局 Z、
  布尔运算或跳过验证器。
- `DesignSpec` 是导出后的审计记录，不反向驱动几何。
- 先用现有 OSM/DEM 做确定性改进；高德、Google 等多源融合暂不进入关键路径。
- 默认打印配置必须保持当前输出兼容；每个阶段用城市样品和 3MF 验证后再改变默认值。

## 2. 统一物理模型

### 2.1 PrinterProfile

第一阶段引入唯一的打印机配置对象，至少包含：

- 喷嘴直径；
- 挤出线宽；
- 层高；
- 最小可靠彩色条带宽度；
- 最小可靠间隙；
- 最小表面层数。

所有值都用模型毫米表示并在创建时校验。默认配置对应 0.4 mm FDM 喷嘴，
但不假装等同于某台具体设备的切片器能力。

### 2.2 PrintScale

XY 只使用一个换算：

```text
scale_mm_per_m = model_span_mm / max(bbox_width_m, bbox_height_m)
real_m_per_model_mm = 1 / scale_mm_per_m
```

例如成品有效跨度 196 mm：

| 真实取景 | 0.4 mm 喷嘴对应实地宽度 | 0.42 mm 线宽对应实地宽度 |
|---|---:|---:|
| 5 km | 10.2 m | 10.7 m |
| 15 km | 30.6 m | 32.1 m |
| 25 km | 51.0 m | 53.6 m |

Z 不按真实地理高度与 XY 等比例缩放。建筑、道路、水面和地标使用语义高度，
但所有厚度最终按层高量化，并满足最小表面层数。

## 3. 几何表现模型

### 3.1 三套网络，不再让一份路网承担所有职责

1. **拓扑网络**：尽量完整，仅用于连通性和候选街区构造，不直接渲染。
2. **结构缝网络**：只保留能分隔有效 block 的边界；优先用同材料浅缝表现，
   避免为了切块而把所有小路染成深色粗线。
3. **可见网络**：按道路等级、连通骨架、城市特征和局部墨量预算筛选，才进入
   灰色道路材料。

这样可同时保留 block base 的密度感和清爽的俯视视觉。

### 3.2 水体四级分类

- `major`：海面、大湖、大江，保留真实面形和城市级轮廓；
- `structural`：参与空间分区的主要河渠；
- `minor`：达到打印宽度且对构图有贡献的小河渠；
- `noise`：低于打印尺度或孤立碎片，不进入可见层。

大水体的黑色面积是构图留白，不与细碎道路使用同一种“墨量过多”惩罚。

### 3.3 墨量按语义和局部窗口计算

不再只看整张图的平均黑色占比，分别记录：

- 主水体占比；
- 陆地上的可见道路占比；
- 次级水体占比；
- 建筑/街区占比；
- 滑动窗口局部深色占比的 P95。

局部 P95 用于发现米兰、东京、上海等局部又黑又密的问题；主水体独立计分，
避免芝加哥湖面、东京湾因面积大而被错误削弱。

### 3.4 视觉焦点与 Z

后续引入确定性的 `FocusSpec`：候选来自高建筑、知名地标、河流转折、桥群、
环状/放射状路网中心和海湾轮廓。焦点区保留更多建筑并提高语义高度，背景区
更强聚合、更低高度、更少可见道路。Z 只在建网格前按语义映射和层高量化，
不对最终网格做全局拉伸。

## 4. 分阶段实施

### P0：物理约束基础与可观测性（本分支第一提交）

- 新增 `PrinterProfile`、`PrintScale`、厚度量化函数；
- 默认 profile 复用现有 0.4 mm 喷嘴，不改变默认成品；
- 预处理从 profile 读取喷嘴尺寸；
- 修复画廊 harness 中建筑最小高度等已解析参数的漏传；
- `design_spec.json` 记录打印配置、比例和推导后的真实尺度；
- 单测覆盖 5/15/25 km 换算、非法配置、层高量化和 DesignSpec 序列化。

验收：非慢速测试通过；同一默认输入的喷嘴实地尺寸与旧公式一致；
自定义 profile 能被审计且不会由 LLM 修改。

P0 实测命令：

```bash
.venv/bin/python -m pytest tests/ -m "not slow" -q
# 390 passed, 2 skipped, 11 deselected
```

真实地图烟雾验收（纽约港 5 km）：

```bash
.venv/bin/python generate_city_legacy.py \
  --bbox 40.6903,-74.0357,40.7353,-73.9763 \
  --pbf pbf_cache/new-york-latest.osm.pbf \
  --city print_profile_nyc_5km --auto-params \
  --no-vegetation --no-block-base
```

- 产物：`output/print_profile_nyc_5km/full_print_profile_nyc_5km_0823_1707.3mf`
  （3,967,716 bytes）；
- SHA-256：`0dbd16c295c99776ac10e47d5c1d13c762cf0deae7977281a0217af197705718`；
- 非零证据：道路 1,621；水体 5 WL + 4 WO；建筑 694 BL + 498 BO；
- 项目验证器：`passed=true`，0 errors，0 warnings；
- 同目录 `design_spec.json` 为 schema 1.1，包含完整 printability 报告。

这次烟雾验收刻意关闭植被和 block base，只验证 P0 的真实数据提取、几何接入、
3MF 导出、非零要素证据和审计链路；它不是 P1 道路职责分离的视觉验收样品。

### P1：道路职责分离

- 拓扑路网继续切 block；
- 从相邻有效 block 的公共边界生成结构缝；
- 可见路网单独按等级、连通性和打印宽度筛选；
- 统计输入路数、拓扑路数、结构缝数、可见路数，禁止“零道路但任务成功”。

阻断式验收不再使用单一城市。`data/print_regression_scenarios.json` 固化以下
核心矩阵：

| 城市 | 场景类型 | 必须守住的结构 |
|---|---|---|
| 芝加哥 | 高密棋盘路网 + 大湖 | 湖面、棋盘骨架、建筑密度；去掉防波堤噪声 |
| 北京 | 内陆环状/放射路网 | 环路、中轴、放射结构；支路不能淹没主结构 |
| 上海 | 高密建筑 + 宽江分割 | 黄浦江真实面形、两岸分区、局部墨量 |
| 西湖 | 山水湖泊 + 大江 | 西湖与钱塘江层级、地形、边缘留白 |

四个核心城市都必须生成 15 km 与 25 km 俯视图，并生成 25 km 正式 3MF。
米兰、东京、墨西哥城作为扩展观察组。任意一个必需运行或必需证据缺失，
`evaluate_result_set()` 都会阻止默认参数升级；不能再用单个漂亮样品宣称泛化成功。

P1 实现进度（2026-08-23）：

- `road_roles.py` 已将同一份 OSM 道路分为 topology / structural / visible；
- topology 仍用于构造街区，structural 只负责 block-base 结构缝，
  visible 才进入 PNG、GLB 和 3MF 的对比材料；
- 25 km 级可见道路执行“命名主干道连续分组 + 分级物理墨量预算”，
  互通匝道保留在拓扑中但不默认渲染，避免立交变成黑结；
- 道路宽度下限由成品比例、`min_colored_strip_mm` 和道路等级共同推导，
  同一个已解析倍率保存在 `LayerPolygons.road_roles` 和 `design_spec.json`
  中，预览与正式 mesh 不再各读一份全局参数；
- 画廊缓存 schema 已升级，道路倍率和打印配置均进入指纹。

芝加哥 25 km 首轮实测（同一份 block/base/building 几何，只替换 visible 道路）：

| 证据 | P1 只分职责 | P1 + 打印宽度/墨量 |
|---|---:|---:|
| OSM 线要素 | 242,921 | 242,921 |
| 可见候选 | 13,881 | 13,881 |
| 实际可见线段 | 13,378 | 2,987 |
| 预算选中的原始 feature | 未限额 | 3,106 |
| 估算线宽面积比（重叠重复计算） | 未记录 | 5.19% |

对比图：

- 分职责基线：`output/style_gallery/print_roles_chicago_25km/dense_detail_topdown.png`；
- 打印宽度/墨量原型：`output/style_gallery/print_ink_chicago_25km/dense_detail_topdown.png`；
- 机读证据：`output/style_gallery/print_ink_chicago_25km/road_role_evidence.json`。

这一轮明显减少了线噪声，但不以芝加哥单图宣告默认值可用。
北京的环状/放射结构与上海的江面/两岸密度仍是阻断验收项。

P1 当前测试：

```bash
.venv/bin/python -m pytest -m 'not slow' -q
# 439 passed, 2 skipped, 11 deselected
```

P1 正式 25 km 阶段证据（2026-08-23）：

| 城市 | 节点 | 总耗时 | 3MF 大小 | 道路 `source → topology → visible` | 严格验证 | 视觉结论 |
|---|---|---:|---:|---:|---|---|
| 北京 | Windows WSL | 286 s | 91.41 MB | 53,715 → 34,572 → 824 | V1–V12 全通过，0/0 | 几何合格；封闭黑线偏重，环状/放射特征仍不够集中 |
| 上海 | Intel Mac | 1,874.3 s | 89.21 MB | 51,536 → 39,112 → 1,557 | V1–V12 全通过，0/0 | 黄浦江 S 弯和两岸分区清楚；边缘分割线仍偏黑 |
| 西湖 | Windows WSL | 122.8 s | 48.80 MB | 44,420 → 21,646 → 917 | V1–V12 全通过，0/0 | 钱塘江连续且西湖可辨；黑色地块边界仍影响高级感 |
| 芝加哥 | controller / Windows 对照 | 运行中 | — | — | — | 同 bbox、同 profile 对照 Block base 热点优化 |

每个已完成目录同时保存 `design_spec.json`，记录 artifact SHA-256、实际 bbox、
打印 profile、要素证据与 V1–V12 结果。以上 0/0 只证明当前验证器覆盖的几何约束；
它不等价于视觉优秀，也不能证明所有语义水层都存在。当前正式水体网格仍以统一材料
的 0.4 mm 平底水板为主，V8/V9 通过主要证明该水板闭合且有厚度。

这批实测还发现两个独立性能问题：

- 非交互 SSH 的 `PATH` 漏掉 `/usr/local/bin`，使 Intel Mac 在原生 osmium 已安装时
  仍走 portable pyosmium；现已显式探测标准 Homebrew 路径。该次上海 portable
  提取仍得到 51,536 条道路，证明回退不是“零道路但无报错”。
- Block base 网格在同一 cell 上反复对复杂 exclusion 做 `intersects`。现在先 union、
  再 `shapely.prepare()`，并把 prepared geometry 放在谓词第一操作数。10,000 个
  合成 block 的定向微基准从 1.0434 s 降至 0.0215 s，布尔结果完全一致；城市级
  加速幅度以正在运行的芝加哥同输入对照为准，不把微基准倍数外推到整条 pipeline。

默认参数尚未升级：四城 15 km 阵列仍未补齐，且可见道路之外的深色封闭分割线
尚未完成语义拆分。现阶段结果属于 P1 验收证据，不自动替换网站首页样品或生产默认值。

### P2：水体层级与墨量预算

- 用面形、面积占比、主轴宽度、边界接触和连通性识别大水体；
- 线状水体按显式 `width`、等级和打印尺度处理；
- 实现分层墨量和局部 P95；
- 海岸/湖岸回归覆盖纽约、芝加哥、东京、上海、西湖。

P2 第一切片进度（2026-08-23，`agent/city-identity-composition`）：

- 删除“所有水体按单个 feature 排序后只留 500 个”的全局截断。线状水体完整
  保留到结构层；只有异常多的普通 polygon 才受独立安全上限约束，短连接段不会再
  因面积排名从河网中间消失。
- 新增 `water_roles.py`：命名河流/运河先按语义 identity 合并成完整走廊，再按
  跨画幅程度、二维展开和物理墨量选择；同一走廊内小于两个喷嘴实地宽度的端点断口
  才允许确定性补连。
- 普通小水面不再因为“勉强达到一个可打印点”就自动进入纯黑层；25 km 场景要求
  大约三个喷嘴足迹的面积，而命名或大型水面仍按 WL 保留。
- 道路选择升级为 `print-road-roles-v3`：环状、放射和跨画幅走廊优先；墨量预算后
  只补回两端都落在已选骨架上的短段。Block base 继续使用更密的 structural 网络，
  因此减少深灰材料不会同步抹掉街区密度。
- GLB 预览只消费 `preprocess_layers` 已解析的 WL/WO，不再把被视觉策略淘汰的原始
  LineString 水网偷偷重新加回；PNG、GLB 与正式 terrain cutter 使用同一选择结果。

北京 25 km 首轮对照（同 bbox、同打印 profile、同参数覆盖）：

| 指标 | P1 旧结果 | 城市身份第一切片 |
|---|---:|---:|
| 水体源进入预处理 | 500 / 1546 | 1528 / 1528（裁框后） |
| 命名水道候选 → 已选走廊/线段 | 103 raw lines | 126 → 11 groups / 13 merged lines |
| 普通水面 WO（过滤前） | 85 | 52 |
| 可见道路（最终） | 824 | 671 |
| GLB 原始水线回灌 | 发生（925 条） | 0；只用 WL/WO |

评审图：

- P1：`output/print_p1_beijing_25km_shellfix/print_p1_beijing_25km_shellfix_topdown.png`
- 城市身份第一切片：
  `output/identity_beijing_25km_v2/identity_beijing_25km_v2_topdown.png`

结论限定：北京的纯黑水网和深灰道路已明显减量，预览/正式语义也已对齐；但北京
环路与中轴是否足够突出仍需上海、芝加哥同策略对照后再判断，当前切片不直接升级
生产默认值。

### P3：视觉中心与语义 Z

- 实现 `FocusSpec` 候选与确定性打分；
- 焦点区/过渡区/背景区分别控制建筑保留、聚合与高度；
- 所有层厚量化到整数层并输出层数报告；
- 用切片器验证细条、间隙、首层融合和材料切换。

### P4：默认参数升级

- 对 15 km 与 25 km 分别建立黄金样品；
- 盲评俯视图、3D 预览和切片结果；
- 只有在代表城市无系统性退化、3MF 验证器 0 errors / 0 warnings 后，
  才切换 Web 默认策略。

## 5. 第一阶段不做的事

- 不重写现有全部 mesh builder；
- 不一次性替换所有城市参数；
- 不以 PNG 好看代替 3MF 可打印验收；
- 不删除现有样品、缓存或线上结果；
- 不把当前 Linux 云服务器耗时外推为 16 GB Mac 的性能结论。
